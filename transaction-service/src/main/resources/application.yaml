spring:
  main:
    web-application-type: none
  application:
    name: transaction-service
  datasource:
    url: jdbc:postgresql://localhost:5433/bankadb
    username: banka
    password: banka123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082